# 时序模块验收记录

运行日期：2026-09-01。分支：`codex/factorynet-temporal-workbench`。

## 真实 FactoryNet 构建

- 文件：`cnc_000.parquet`
- SHA-256：`4754f0389c31a13fc84ace615a77d63aa3483ed49b4ab503d70b4c68a4519e6d`
- 源记录：25,286；字段：57；episode：18
- 默认构建：选中 5,000；规范化 5,000
- 结果：5,074 节点、25,057 条关系、0 个时间问题
- 时间语义：Ordinal，原始 `time_s` 范围 0.0–232.6，按 episode 生成 `event_seq`
- M3：真实调用 MiniMax-M3，画像状态 completed，响应哈希记录在 TemporalDatasetProfile
- 幂等性：相同配置第二次运行后节点总数仍为 5,074

## 清理

`scripts/reset_temporal_runtime.py --dry-run` 仅命中旧 ICEWS/错误时序运行；`--apply --yes` 已执行。FactoryNet 和 C-MAPSS 常规链路保留，旧 ICEWS 本体、运行、证据和图已删除。备份清单写入 `/tmp/nano-temporal-backups/`。

## 构建验证

- 后端 Python 编译检查：通过。
- 前端 `npm run build`：通过（仅有已有 bundle 体积提示）。
- 浏览器入口：显示五步进度、FactoryNet 卡片、MiniMax 状态和历史任务；未选择数据时“下一步”禁用。
